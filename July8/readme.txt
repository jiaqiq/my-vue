
1.forEach(function(val, ind, arr) {
	
}, thisArg)

2.Math.max.apply(null, []);

3. function sortArr() {
	return a - b;
}

var arr1 = [1, 3, 2];

arr1.sort(sortArr);

4.push() 后添加， 返回新长度
  pop()  后移除， 返回移除项
  unshift() 前添加  返回新长度
  shift() 前移除   返回移除项
